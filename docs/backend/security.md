# Royal Vault Security Model

## Zero-Knowledge Architecture

- Encryption keys never leave the client
- Vaults are encrypted before upload
- Server stores ciphertext only

## Threat Model

Even if the database is compromised, attacker gains nothing.