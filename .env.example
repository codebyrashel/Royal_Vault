# Example environment configuration for Royal Vault

# Database
DB_HOST=localhost
DB_PORT=5433
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=royal_vault
DB_SSLMODE=disable

# Server
PORT=8080

# JWT secret key (use a strong random value in real environments)
JWT_SECRET=your_jwt_secret_here